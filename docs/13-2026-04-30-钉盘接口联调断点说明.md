# 13-2026-04-30-钉盘接口联调断点说明

## 一、当前结论

本轮钉盘联调已确认：

1. `appKey / appSecret -> access_token` 链路可用；
2. `userid -> unionId` 链路可用；
3. 已知 `fileId` 场景下：
   - `download-info-sdk` 可用；
   - `download-file-sdk` 可用；
   - 实际文件已成功下载到本地；
4. 公开 `drive/files` 文件列表接口在当前应用下会报缺少 `Drive.File.Read`；
5. 改走 `storage/dentries` 后，手工调试接口已成功拿到根目录日报 Excel 列表；
6. 当前主目标已切换为：基于钉盘最新日报文件，驱动数据页内容，而不是继续停留在单文件下载验证。

## 二、已确认可用的接口/能力

### 1. 用户身份与下载链路

- `/api/integrations/dingtalk/userid-to-unionid?userId=135529275`
- `/api/integrations/dingtalk/download-info-sdk?unionId=...&spaceId=28756861180&fileId=...`
- `/api/integrations/dingtalk/download-file-sdk?unionId=...&spaceId=28756861180&fileId=...&fileName=...`

### 2. 文件列表调试链路

手工验证通过：

- `/api/integrations/dingtalk/raw-get?path=/v1.0/storage/spaces/28756861180/dentries&query=unionId=...&parentId=0&maxResults=50&refreshToken=true`

该接口返回了 20260423 ~ 20260428 多个日报 Excel 的 `id / name / modifiedTime / path`。

## 三、最新定位结果

### `/api/workbench/daily-report/latest-sheet` 的组合流程已在新代码中打通

本轮已在后端补充：

- `/api/workbench/daily-report/latest-sheet-debug`

用于直接返回组合流程中的中间状态，包括：

- `resolvedFolderId`
- `listedFileCount`
- `excelFileCount`
- `excelFiles`
- `selectedFile`
- `selectedBy`
- `resolvedReportDate`
- `downloadedBytes`
- `workbookSheetCount`
- `workbookSheetNames`
- `matchedSheetName`
- `matchedBy`
- `confidence`
- `candidates`
- 失败时的 `errorType / errorCode / errorMessage`

对真实业务参数实测结果表明，这条组合链路已经可以成功走通，而不是继续卡在黑盒内部：

- `resolvedFolderId = 0`
- `listedFileCount = 11`
- `excelFileCount = 11`
- 选中的最新文件为：
  - `fileId = 219723694984`
  - `name = EXPERLAM-美国-流量数据统计报表-20260428.xlsx`
  - `modifiedTime = Thu Apr 30 09:14:42 CST 2026`
- `downloadedBytes = 44907`
- `workbookSheetCount = 26`
- 已成功匹配到目标 sheet，置信度为 `high`

因此，当前 `unknownError` 的高概率根因已经从“业务链路内部异常”转移为“运行中的 8080 服务并不是最新代码实例”。

### 当前高概率真实阻塞

1. 8080 上的后端仍是旧进程，未加载新增的 `latest-sheet-debug` 路由；
2. `mvn package` 时 target 下 jar 无法重命名为 `.original`，说明旧 Java 进程仍占用该 jar；
3. 用户/前端实际访问的仍是旧服务，因此继续看到旧的 `unknownError`。

## 四、代码现状

### 后端已新增/改动

- `integration/dingtalk/DingTalkSdkService.java`
- `integration/dingtalk/DingTalkDriveClient.java`
- `service/DailyReportService.java`
- `controller/IntegrationDebugController.java`
- `controller/WorkbenchController.java`
- `config/WebClientConfig.java`
- `resources/application.yml`
- `pom.xml`

### 前端已改动

- `frontend/src/api/workbench.ts`
- `frontend/src/views/ProductTaskDetailView.vue`

当前前端逻辑为：

- 优先请求后端最新日报接口；
- 若失败，则回退到静态 `dailyReportSheets.ts`。

## 五、下一步建议

优先执行：

1. 停掉当前 8080 的旧后端进程；
2. 重新启动最新代码实例；
3. 先验证：
   - `/api/workbench/daily-report/latest-sheet-debug?...`
4. 再验证：
   - `/api/workbench/daily-report/latest-sheet?...`

若 8080 成功切到新代码，按当前实测结果，`latest-sheet` 大概率即可恢复正常。

## 六、本轮新增代码能力

已完成以下增强：

1. `DailyReportService` 已新增调试型组合流程输出；
2. `WorkbenchController` 已新增：
   - `/api/workbench/daily-report/latest-sheet-debug`
3. 调试接口已支持可选参数：
   - `latestReportFileId`

因此后续即使再次失败，也可以明确看到失败步骤，而不再是纯黑盒排查。